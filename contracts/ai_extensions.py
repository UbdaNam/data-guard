"""Deferred AI-specific contract extension entry point.

Feature 1 preserves the stable interface boundary only.
"""

from __future__ import annotations


def main() -> None:
    raise NotImplementedError("AI-specific checks are deferred to later features.")
